// Showed creativity by adding a function called GetRank(). 
// This looks at the scores and determines what rank they are based on that scores, and outputs it to the console.
// It is like grades where there is an A, B, C, D, and F. Ranks...except it is based on the score.
using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();

        goalManager.Start();
    }
}